Making aliases (shorthand for longer commands):

`alias ll='ls -la'``


# Useful aliases to have in .bash_profile:

alias ll='ls -la'
alias cl='curl -L'
alias ..='cd ..'
alias ...='cd ...'
alias now='date +"%T"'
alias sl='ls'
